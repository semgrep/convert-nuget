{
  "name": "convert-nuget",
  "version": "1.0.0",
  "description": "Recursively searches for `packages.config` files and converts each to `packages.lock.json`. The generated files are created in the same directory as each discovered `packages.config` file.",
  "main": "convert-nuget.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/semgrep/convert-nuget.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/semgrep/convert-nuget/issues"
  },
  "homepage": "https://github.com/semgrep/convert-nuget#readme",
  "dependencies": {
    "fast-xml-parser": "^5.3.1"
  }
}
